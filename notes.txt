Comandos:

npm  install -g @angular/cli@16
ng --help
ng version
ng new chess
ng generate component components/input
npm uninstall -g @angular/cling g service domain/api
ng g guard domain/AuthGuard

Two way binding:

import { FormsModule } from '@angular/forms';
--
[(ngModel)]="text"

Signal:
mostrar algo na tela que seja atualizado - integra

// rastreabilidade de uma variável
// monitorar alterações
export class App {
  protected readonly title = signal('rplace');
}

Stand Alone Component:
cria um componente não tendo um módulo principal
-> componentes do angular

ponte é feita com o httpclient
envia pacotes entre front e back -> subscribe envia
observable monitora esse processo e cuida dos acessos
pipe fecha a ponte

Competencias cobradas:


1. Compreender Arquitetura Angular e seus comandos
2. Elaborar componentes (templates e bindings)
3. Produzir formulários reativos utilizando forms
4. Utilizar o conceito de SPA (single page application)
5. Compreender funcionamento de componentes 'standalone'
6. Aplicar ferramentas de otimização de renderização reativa (signals)
7. Aplicar ferramentas de operações assíncronas (observables)
8. Utilizar modulação para fazer requisições https

*SE DER TEMPO*
9. Implementar conexões frontend e backend com serviços de websocket (RPLACE)
10. Implementar conexões frontend e backend com serviços de messageria
