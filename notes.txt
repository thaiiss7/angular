Comandos:

npm  install -g @angular/cli@16
ng --help
ng version
ng new chess
ng generate component components/input
npm uninstall -g @angular/cling g service domain/api
ng g guard domain/AuthGuard

Two way binding:

import { FormsModule } from '@angular/forms';
--
[(ngModel)]="text"

Signal:
mostrar algo na tela que seja atualizado - integra

// rastreabilidade de uma variável
// monitorar alterações
export class App {
  protected readonly title = signal('rplace');
}

Stand Alone Component:
cria um componente não tendo um módulo principal
-> componentes do angular

ponte é feita com o httpclient
envia pacotes entre front e back -> subscribe envia
observable monitora esse processo e cuida dos acessos
pipe fecha a ponte
